# 总体建议

## 方案状态与决策优先级

本文件最初用于把单页 Nereus 网站迁移到 Docusaurus。该迁移已经完成；从本次修订开始，后续实施目标升级为：

> **把 `nereusstream.github.io` 建设为 NereusStream 组织官网和可扩展的多产品文档门户。**

本次修订于 2026-08-07 生效。凡是旧章节中把 `Nereus` 当作全站品牌、把 `/docs` 默认等同于 Nereus 文档、把全局 GitHub 链接指向单个 Nereus 仓库的内容，均以本次多产品方案为准。旧方案中关于 Nereus 架构、PDF 迁移、内容覆盖、正确性边界和文档基线的内容仍然有效，但其页面归属从“组织首页/全站文档”调整为“Nereus 产品页/Nereus 文档实例”。

品牌和产品层级固定为：

```text
NereusStream                      组织与顶层品牌
├── Nereus                       旗舰产品：共享流存储
├── Nereus Delay                 产品：持久延迟消息调度
└── Future products              后续产品，按同一模型接入
```

界面展示名称统一使用 `Nereus Delay`；仓库名、URL slug 和代码标识继续使用 `nereus-delay`。不要把 Nereus Delay 表达为 Nereus 的一个文档章节、插件或附属功能，它与 Nereus 是 NereusStream 下的同级产品。Nereus 可以拥有更高的视觉权重和更丰富的内容，但不能继续充当全站品牌。

## 当前实施基线

> **实施同步（2026-08-07）**：单产品站点迁移阶段已经完成。站点使用 Docusaurus 3.10.2、TypeScript、React 18、Mermaid、显式 sidebar 和 GitHub Pages 工作流；原静态首页已归档到 `legacy/index.html`。多产品批次 1 至批次 4 已完成：当前 `/` 是 NereusStream 组织首页，`/products/nereus/` 与 `/products/nereus-delay/` 是独立产品页；`/docs/` 是 Docs Hub，Nereus 正文位于 `/docs/nereus/`，Nereus Delay 正文位于 `/docs/nereus-delay/`，旧 Nereus 文档路径由 `migration/legacy-redirects.ts` 生成一跳重定向。Delay 产品页、source lock、content map、独立 sidebar 和最小公开阅读路径均已接入。

> **多产品实施同步（2026-08-07）**：批次 0 已于 `b3f6a25` 完成并推送；批次 1 的品牌与产品数据基础、批次 2 的组织首页与 Nereus 产品页、批次 3 的 Nereus 文档命名空间、批次 4 的 Nereus Delay 来源核验和最小文档实例已在当前工作树完成并通过 `typecheck` 与 `build`。`src/data/products.ts` 驱动 Products dropdown、产品卡片、Docs Hub 和 Footer；两个 docs plugin 分别使用 `/docs/nereus/` 与 `/docs/nereus-delay/`。Delay 页面和文档明确保留 queued/applied/published/uncertain、`deliverAt`/`actionAt`、at-least-once、Lane isolation、Recovery Floor 与 release blocker 边界，不把当前实现状态写成 V1 release-ready。

Nereus 文档当前基线为 `nereus main@c820391d`（2026-08-07 核验）；PDF 输入快照仍为 `nereus main@894fc4e`（2026-08-04 生成），`migration/nereus/pdf-content-map.yml` 的 English chapters、appendices、figures 和 tables 已无未映射项。多产品改造不得破坏这一覆盖结果，迁移目录后必须同步更新映射目标路径。

Nereus Delay 当前权威材料位于产品仓库；本次方案核验的干净工作树基线为 `main@9281890f42772cc01b6b2b607fd93e31de64879b`（2026-08-07 核验）。产品定位为面向 Kafka 和 Pulsar destination 的 Java 21 持久延迟消息调度 library/service core。其 V1 语义与协议基线已经冻结，但实现状态文档仍列有 release blocker，因此网站状态只能表达为 `V1 in development` 或更具体的已核验状态，不能表达为 stable、production-ready 或 complete。

## 总体实施原则

1. `/` 只回答 NereusStream 是谁、有哪些产品以及从哪里进入，不承载任何单一产品的完整架构说明。
2. Nereus 是首页主推产品，但 Nereus 与 Nereus Delay 都拥有独立产品页、独立文档入口、独立状态和独立源码基线。
3. `/docs` 是文档门户；产品正文分别进入 `/docs/nereus/` 和 `/docs/nereus-delay/`。
4. 已发布的 Nereus `/docs/...` URL 必须提供重定向，不能通过迁移制造 404。
5. 产品列表、导航、Footer、首页卡片和状态入口由统一数据模型驱动，新增产品不应要求复制整套页面结构。
6. 网站是面向读者的解释层；产品仓库中的精确协议、ADR 和实现证据保持各自权威，不得通过网站摘要放宽。
7. 每一批实现都同步更新本文件、source baseline、content map、状态说明和重定向清单，并在通过对应检查后单独 commit、push。
8. PDF 是产品文档的发布快照，不再作为全站唯一输入；不同产品可以有独立快照和版本节奏。

---

# 一、应该与 `pulsar-site` 对齐到什么程度

继续采用与 `pulsar-site` 相近的基础工程能力，但信息架构必须按 NereusStream 的产品规模设计。

| 层次 | 应对齐的内容 | NereusStream 的具体取舍 | 不应复制的内容 |
| --- | --- | --- | --- |
| 工程框架 | Docusaurus 3、TypeScript、React、Markdown/MDX、CSS Modules | 保持当前 Docusaurus 3.10.2 基线 | Bootstrap、MUI、jQuery 和历史兼容代码 |
| 文档体验 | 左侧目录、右侧 TOC、上下页、Edit this page、Mermaid、搜索、语言 | 为 Nereus 与 Nereus Delay 建立两个 docs plugin instance | Pulsar 的数百篇目录和全部历史版本 |
| 发布治理 | PR 构建检查、GitHub Pages、稳定 URL、重定向 | 产品文档独立 source lock 和状态验证 | ASF Footer、Apache 发布流程 |
| 首页结构 | React 组件化、统一 Layout、移动端导航 | 组织 Hero + 数据驱动产品展示 | 对 Pulsar 首页像素级复制 |
| 内容组织 | Overview、Concepts、Operations、Reference | 每个产品独立 sidebar，`/docs` 作为产品选择页 | 与当前产品无关的 Functions、IO、Clients 分类 |

技术基线保持：

* Docusaurus `3.10.2`
* Node.js `>=22`
* React 18
* TypeScript
* `@docusaurus/preset-classic`
* `@docusaurus/plugin-content-docs`
* `@docusaurus/plugin-client-redirects`
* `@docusaurus/theme-mermaid`

多产品需要多个 docs plugin instance，但不需要复制 Pulsar 的复杂预处理器、多代版本并行构建或大量历史重定向逻辑。当前只实现两个产品实例和一份可审计的 legacy redirect manifest。

---

# 二、全局导航、品牌和 Footer

## 2.1 桌面端导航

最终全局导航固定为：

```text
[NereusStream mark] NereusStream   Products ▾   Documentation        Theme   GitHub
```

导航职责如下：

| 项目 | 行为 | 目标 |
| --- | --- | --- |
| 品牌区 | 点击返回组织首页 | `/` |
| Products | 下拉显示全部已发布产品 | 产品页 |
| Documentation | 进入产品文档选择页 | `/docs/` |
| Theme | 使用 Docusaurus 内置明暗模式 | 当前页面 |
| GitHub | 指向组织主页，不指向单一产品 | `https://github.com/nereusstream` |

`Products` 下拉首期使用 Docusaurus 内置 dropdown，只包含短名称和目标：

```text
Nereus       → /products/nereus/
Nereus Delay → /products/nereus-delay/
```

完整产品描述放在组织首页和 Docs Hub，不为两项菜单提前自定义复杂 mega menu。

不在全局导航平铺 `Nereus`、`Nereus Delay` 和未来每一个产品，否则产品增加后会再次遇到空间不足。产品仓库的 GitHub 链接放在产品页和对应文档入口，不占全局导航。

Search 在两个产品至少都有可用文档后加入。语言切换只在英文和中文的组织首页、Docs Hub 与两个产品 Overview 都完整后展示。版本下拉不能放成一个含义不明的全局版本；如果将来启用版本化，应在对应产品文档上下文中显示。

## 2.2 品牌锁定与左上角尺寸

现有 `nereus-icon.png` 的自然宽高比约为 1.98，应作为 NereusStream mark 使用。导航不得把它压缩为错误比例，也不得直接使用纵向排列的 `nereus-logo.png` 作为窄导航图片。

首期保持“真实图片 mark + DOM 文字”的组合：

```ts
navbar: {
  title: 'NereusStream',
  logo: {
    alt: 'NereusStream',
    src: 'img/nereus-icon.png',
    width: 42,
    height: 21,
  },
}
```

CSS 约束：

* `.navbar__brand` 使用稳定 gap，不通过图片内透明留白制造间距。
* `.navbar__logo` 使用 `height: auto` 和 `object-fit: contain`，不拉伸。
* `.navbar__title` 使用 `white-space: nowrap`，不能在 `Nereus` 与 `Stream` 之间换行。
* 品牌区在 320px 宽度仍需完整可见；空间不足时先收起导航项进入移动菜单，不能裁切品牌。
* Logo alt、GitHub aria-label 和菜单键盘焦点必须存在。

后续如果制作横向官方 lockup，应提供浅色和深色背景版本，并在替换前比较 320、375、768、1440px 四种宽度；在此之前不需要重新生成品牌 Logo。

## 2.3 导航配置形态

全局 `Documentation` 必须指向 Docs Hub，不能继续使用单一 `docSidebar` 直接进入 Nereus：

```ts
items: [
  {
    type: 'dropdown',
    label: 'Products',
    position: 'left',
    items: [
      {label: 'Nereus', to: '/products/nereus/'},
      {label: 'Nereus Delay', to: '/products/nereus-delay/'},
    ],
  },
  {
    label: 'Documentation',
    to: '/docs/',
    position: 'left',
  },
  {
    href: 'https://github.com/nereusstream',
    position: 'right',
    className: 'header-github-link',
    'aria-label': 'NereusStream GitHub organization',
  },
]
```

移动端菜单保持同一信息顺序：Products、Documentation、GitHub。桌面和移动端首期都只显示产品短名称，避免引入额外导航组件和窄屏高度问题。

## 2.4 Footer

Footer 调整为三组真实入口，不放尚不存在的 Community、Blog、Download 或 Quickstart：

```text
Products
├── Nereus
└── Nereus Delay

Documentation
├── Documentation home
├── Nereus docs
└── Nereus Delay docs

Organization
├── GitHub
└── nereusstream.github.io source
```

版权主体保持 `NereusStream`。Footer 中不能出现把 Nereus 仓库称为整个组织 GitHub 的链接。

---

# 三、页面信息架构与首页设计

## 3.1 稳定 URL

最终公开路由固定为：

| 页面 | Canonical URL | 主要用途 |
| --- | --- | --- |
| 组织首页 | `/` | 组织定位和产品发现 |
| 产品总览 | `/products/` | 可选；产品超过两个后启用，首期可以重定向到首页产品区 |
| Nereus 产品页 | `/products/nereus/` | Nereus 定位、架构摘要、能力和状态 |
| Nereus Delay 产品页 | `/products/nereus-delay/` | Delay 定位、流程、语义边界和状态 |
| 文档门户 | `/docs/` | 选择产品文档 |
| Nereus 文档 | `/docs/nereus/` | Nereus 文档首页与 sidebar |
| Nereus Delay 文档 | `/docs/nereus-delay/` | Nereus Delay 文档首页与 sidebar |

所有内部链接统一使用 trailing slash 兼容当前 GitHub Pages 配置。旧 Nereus `/docs/...` 路径只作为重定向来源存在，迁移完成后不得继续出现在正文、导航、Footer 或首页数据中。

## 3.2 组织首页目标

组织首页只回答四个问题：

1. NereusStream 是什么。
2. 当前维护哪些产品。
3. 每个产品解决什么问题、处于什么阶段。
4. 应该进入哪个产品页、文档或仓库。

首页不再解释 Nereus 的五种 Storage Profile、generation、head CAS、BookKeeper/Object WAL，也不展示 Nereus 的 source SHA 和 PDF SHA。这些内容属于 Nereus 产品页或文档。

### 第一屏：Organization Hero

首期使用以下稳定文案：

```text
NereusStream

Infrastructure for durable streams and scheduled delivery.

NereusStream builds infrastructure for Apache Pulsar and Apache Kafka—from
shared stream storage to durable delayed-message scheduling.

[Explore products] [View on GitHub]
```

`Explore products` 滚动到 `#products`，`View on GitHub` 指向组织主页。Hero 不使用某个产品的版本号或测试数量。右侧视觉首期使用真实 NereusStream mark 和两个产品摘要面板，不继续使用只表达 Nereus storage core 的 orbit 图。

### 第二屏：Products

这是首页最重要的内容区，必须紧跟 Hero。

桌面端使用 2:1 或 3:2 的视觉权重：Nereus 为 featured card，Nereus Delay 为标准 card；移动端按 Nereus、Nereus Delay 顺序垂直排列。产品数量以后增加时由 CSS Grid 自动换行，不显示空的 `Coming soon` 占位。

Nereus 卡片内容：

```text
Flagship · v0.1.0 testing
Nereus
Shared stream storage for Pulsar and Native Kafka.

Stable logical offsets, explicit commit boundaries, physical generations,
recovery, retention, and safe reclamation beneath protocol-facing brokers.

[Explore Nereus] [Read Nereus docs] [GitHub]
```

Nereus Delay 卡片内容：

```text
V1 in development
Nereus Delay
Durable delayed-message scheduling across Kafka and Pulsar destinations.

Managed scheduling, cancellation and rescheduling with durable shard state,
destination isolation, explicit uncertainty, and checkpoint-based recovery.

[Explore Nereus Delay] [Read Delay docs] [GitHub]
```

状态标签必须来自统一产品数据，不能分别硬编码在首页、产品页、Footer 和文档。Nereus Delay 的状态描述必须由其 `IMPLEMENTATION-STATUS.md` 核验；存在 release blocker 时不得显示 `Ready`。

### 第三屏：Organization principles

只展示同时适用于当前产品、且能由产品文档支持的组织级原则：

| 原则 | 首页短文案 | 禁止扩大成的承诺 |
| --- | --- | --- |
| Protocol-aware | Work with Kafka and Pulsar semantics instead of hiding their boundaries. | 不承诺所有协议或所有 Broker |
| Explicit correctness boundaries | Distinguish durable writes, logical commits, queued commands, applied state, and uncertain outcomes. | 不承诺通用 exactly-once |
| Recoverable state | Design recovery, replay, checkpoints, retention, and cleanup as first-class paths. | 不承诺所有场景零数据损失 |

如果某项原则未来不能同时覆盖所有产品，应移到相应产品页，不要留在组织首页。

### 第四屏：Documentation gateway

展示两个产品文档卡片，并提供统一 `Browse all documentation` 按钮进入 `/docs/`。卡片只展示各产品的第一阅读入口和当前 baseline 日期，不在组织首页展示完整 SHA。

### 第五屏：Organization CTA 与 Footer

结束区只提供 GitHub organization 和文档入口。首期不增加虚构的客户 Logo、用户数量、性能数字、Newsletter、Slack 或 Roadmap。

## 3.3 Nereus 产品页

当前 `src/pages/index.tsx` 的内容迁移到 `src/pages/products/nereus/index.tsx`，当前 CSS Module 同步移动到该目录。迁移时保留并重新核验：

1. Nereus Hero 和 `v0.1.0 · testing` 状态。
2. Pulsar/Kafka 到 Nereus storage core 的架构摘要。
3. Stable logical coordinates、one logical commit point、physical generations 三个不变量。
4. 五种 Storage Profile。
5. Nereus 文档阅读顺序。
6. 当前源码与 PDF baseline 状态面板。

页面 CTA 改为：

```text
[Read Nereus docs] → /docs/nereus/
[Nereus on GitHub] → https://github.com/nereusstream/nereus
```

页面 metadata 必须是 Nereus 产品级，而不是全站 metadata：

```text
title: Nereus | NereusStream
description: Shared stream storage for Pulsar and Native Kafka.
```

## 3.4 Nereus Delay 产品页

新增 `src/pages/products/nereus-delay/index.tsx`。第一版必须来自 Nereus Delay 设计、README、术语和实现状态，不从产品名推测能力。

页面顺序：

1. **Hero**：产品定位、`V1 in development`、Docs/GitHub CTA。
2. **What it does**：Schedule、Cancel、Reschedule、Payload Commit 与 Query 的边界摘要。
3. **End-to-end flow**：SDK → Kafka/Pulsar Command Topic/Shard Log → Delay Shard/RocksDB → Scheduler → Kafka/Pulsar destination；Oxia 提供 config/placement/lease/catalog，Object Store 提供 checkpoint/large payload。
4. **Timing semantics**：明确 `deliverAt` 是消费者最早可见时间，不是 publish start time，也不是精确时刻保证。
5. **Delivery boundaries**：区分 queued、applied、published、uncertain；展示 baseline at-least-once 和可能重复，不制造 exactly-once 承诺。
6. **Isolation and recovery**：Destination Lane 隔离、一 shard 一 RocksDB、Recovery Set/Floor/checkpoint。
7. **Current status**：展示规范 revision、源码 commit、核验日期、已实现范围和 remaining release blockers 的入口。
8. **Documentation gateway**：Overview、Concepts、Architecture、Implementation Status。

页面 metadata：

```text
title: Nereus Delay | NereusStream
description: Durable delayed-message scheduling across Kafka and Pulsar destinations.
```

产品页面只能摘要精确协议，不复制完整 Registry 数值表。完整字段、enum、canonical bytes 和 stable code 应进入版本固定的 Reference 文档或链接到对应源码 commit。

## 3.5 Docs Hub

新增 `src/pages/docs/index.tsx`，它是普通 React page，不属于任何产品 docs plugin，从而避免 `/docs/` 与某个产品实例冲突。

页面内容：

* 标题 `Documentation`。
* 一句说明：选择产品后进入各自的概念、架构、操作和参考文档。
* Nereus 卡片：定位、状态、baseline 日期、`Start with Nereus`。
* Nereus Delay 卡片：定位、状态、baseline 日期、`Start with Nereus Delay`。
* `Source and documentation policy` 链接，解释网站摘要、产品源码、精确协议和 PDF 快照的权威关系。

Docs Hub 不展示一个合并侧边栏；进入产品实例后才显示该产品 sidebar。

## 3.6 数据驱动的产品模型

新增 `src/data/products.ts`，它是组织首页、Products dropdown、Docs Hub 和 Footer 的共享展示源。建议类型：

```ts
export type ProductStatus = 'testing' | 'in-development' | 'stable';

export interface ProductDefinition {
  id: 'nereus' | 'nereus-delay';
  name: string;
  slug: string;
  summary: string;
  description: string;
  status: ProductStatus;
  statusLabel: string;
  featured: boolean;
  accent: 'cyan' | 'indigo';
  productPath: string;
  docsPath: string;
  repositoryUrl: string;
  sourceCommit: string;
  lastVerified: string;
}
```

约束：

* `id`、`slug` 和公开 URL 一经发布不得静默修改。
* `featured` 首期只有 Nereus 为 `true`；不得通过数组顺序隐式判断旗舰产品。
* `statusLabel` 可以比枚举更具体，但不能与产品状态文档冲突。
* `sourceCommit` 使用完整 40 位 SHA；UI 可以缩写显示。
* 产品页的详细状态可使用独立文件，但全局摘要必须从该模型读取。
* 新增产品时必须同时提供产品页、Docs Hub 目标、仓库链接、状态和 baseline；不允许只有一张营销卡片。

## 3.7 视觉系统

NereusStream 继续使用当前深蓝、青色、蓝色和 Inter/system font 体系，避免本轮同时重做品牌。

| 层级 | 视觉规则 |
| --- | --- |
| NereusStream | 深蓝背景、蓝青渐变 mark、组织级中性色 |
| Nereus | 现有 cyan/teal 强调色，保留 storage architecture 视觉语言 |
| Nereus Delay | 使用现有 palette 中的 indigo/violet 作为强调色，用于时间和调度语义区分 |
| 状态 | 测试、开发中、稳定采用文字 + 颜色双重编码，不能只靠颜色 |

产品首期不创建两套独立 Logo。组织 mark 始终只代表 NereusStream；产品通过名称、文案、accent 和页面内容区分。所有颜色必须在明暗主题下满足正文和交互控件的可读性要求。

---

# 四、多产品文档架构与内容边界

## 4.1 两个独立文档实例

文档不能继续使用一个 `docsSidebar` 混合所有产品。最终使用两个 Docusaurus docs plugin instance：

| 产品 | Plugin ID | 源目录 | Route base path | Sidebar |
| --- | --- | --- | --- | --- |
| Nereus | `default` | `docs/nereus` | `docs/nereus` | `sidebars.nereus.ts` |
| Nereus Delay | `nereusDelay` | `docs/nereus-delay` | `docs/nereus-delay` | `sidebars.nereus-delay.ts` |

`/docs/` 由 `src/pages/docs/index.tsx` 提供。两个实例不得把自己的 `routeBasePath` 配置为 `docs`，否则会和 Docs Hub 冲突。

两个产品实例需要独立：

* sidebar 和阅读顺序；
* edit URL；
* source baseline 与最后核验日期；
* status 页面；
* 后续版本节奏；
* i18n 内容目录；
* search facet 或搜索上下文。

全局主题、Navbar、Footer、代码高亮、Mermaid 和基础 MDX 组件继续共享。

## 4.2 物理目录迁移

目标目录：

```text
docs/
├── nereus/
│   ├── index.mdx
│   ├── overview/
│   ├── concepts/
│   ├── data-paths/
│   ├── storage/
│   ├── integrations/
│   ├── operations/
│   ├── examples/
│   ├── development/
│   └── reference/
└── nereus-delay/
    ├── index.mdx
    ├── overview/
    ├── concepts/
    ├── data-paths/
    ├── operations/
    ├── development/
    └── reference/
```

实施时使用 `git mv` 保留 Nereus 文档历史：

```text
docs/index.mdx       -> docs/nereus/index.mdx
docs/overview        -> docs/nereus/overview
docs/concepts        -> docs/nereus/concepts
...
```

移动完成后需要同时更新：

1. `sidebars.ts` 拆分为两个文件。
2. 所有 Markdown/MDX 相对链接。
3. 首页、产品页、Footer、Navbar 和数据文件中的绝对路由。
4. `migration/nereus/pdf-content-map.yml` 的 target path。
5. Edit this page 链接。
6. GitHub Pages 旧 URL 重定向。
7. i18n 目录和未来 versioned docs 路径。

## 4.3 Nereus Delay 首批公开文档

Nereus Delay 仓库中的设计与 Registry 很长，第一批网站文档应先建立可读的解释层，而不是一次性复制所有规范文件：

```text
docs/nereus-delay/
├── index.mdx
├── overview/
│   ├── why-nereus-delay.md
│   ├── architecture.md
│   └── reading-guide.md
├── concepts/
│   ├── delivery-time-and-action-time.md
│   ├── commands-messages-and-receipts.md
│   ├── shard-log-and-source-position.md
│   ├── managed-and-auto-fast.md
│   └── delivery-guarantees-and-uncertainty.md
├── data-paths/
│   ├── schedule-flow.md
│   ├── cancel-and-reschedule.md
│   ├── due-claim-and-publish.md
│   └── query-and-read-barrier.md
├── operations/
│   ├── destination-lane-isolation.md
│   ├── ownership-and-activation.md
│   ├── checkpoints-and-recovery.md
│   ├── payload-lifecycle-and-gc.md
│   └── observability-and-release-gates.md
├── development/
│   └── project-status.md
└── reference/
    ├── glossary.md
    ├── authority-order.md
    ├── protocol-registry-guide.md
    └── non-goals.md
```

首批必须至少完成 `index.mdx`、Why、Architecture、Delivery time、Commands/receipts、Schedule flow、Destination Lane isolation、Recovery、Glossary、Authority order 和 Project status。其余页面可以按后续批次补齐，但 sidebar 中不得出现空页面或虚构链接。

## 4.4 Nereus Delay 内容权威顺序

网站必须保留产品仓库已经定义的权威顺序：

1. `Nereus Delay V1 设计.md`：V1 语义、生命周期、恢复、资源和验收基线。
2. `V1-PROTOCOL-REGISTRY.md`：字段号、枚举、canonical bytes、key tag、stable code 和 presence/union 规则。
3. `docs/adr/`：已接受的架构决策和理由。
4. `IMPLEMENTATION-STATUS.md`：当前代码、测试证据和剩余 blocker；不能放宽规范。
5. `V1-DESIGN-AUDIT.md`：跨规范、代码和证据的漂移检查。
6. `CONTEXT.md`：术语速查；不新增规范。

网站摘要发生冲突时，必须修正网站，不能自行选择更宽松的解释。Protocol Registry、完整 ADR 和审计文件首期通过固定 commit 的源码链接提供；如果以后在网站内完整渲染，应由同步脚本导入并校验内容 hash，不能人工复制后独立维护。

## 4.5 Nereus PDF 不要“一章一个超长页面”

这份 PDF 有 21 章和多个附录。它的阅读顺序是刻意安排的：

1. 先讲协议语义和存储语义为什么要分开。
2. 再讲 stream、record、entry、offset。
3. 然后讲 WAL、head、CAS、commit 和恢复。
4. 在理解逻辑提交后再讲 generation。
5. 最后进入 Pulsar、Kafka、cursor、checkpoint、trim 和 GC。

网站侧边栏必须保留这个知识依赖关系，不能简单按文件名字母顺序排列，也不适合把第 9 章 Generation 的十几页内容全部塞进一个页面。

建议拆成大约 30 到 35 个文档页面。

## 4.6 Nereus 文档目录

```text
docs/nereus/
├── index.mdx
│
├── overview/
│   ├── why-nereus.md
│   ├── architecture.md
│   └── reading-guide.md
│
├── concepts/
│   ├── stream-record-entry-offset.md
│   ├── protocol-logical-physical-coordinates.md
│   ├── primary-wal.md
│   ├── stream-head-and-commit-chain.md
│   ├── cas-and-linearization-point.md
│   └── read-target-and-offset-index.md
│
├── data-paths/
│   ├── append-flow.md
│   ├── append-outcomes.md
│   ├── append-recovery.md
│   ├── read-flow.md
│   ├── read-boundaries.md
│   └── index-repair-and-fallback.md
│
├── storage/
│   ├── storage-profiles.md
│   ├── object-wal.md
│   ├── bookkeeper-wal.md
│   ├── generation-overview.md
│   ├── generation-lifecycle.md
│   ├── generation-selection-and-fallback.md
│   └── materialization.md
│
├── integrations/
│   ├── pulsar.md
│   ├── pulsar-cursor-and-subscription.md
│   ├── native-kafka.md
│   ├── kafka-produce-and-fetch.md
│   ├── kafka-recovery-and-compaction.md
│   └── kop-vs-native-kafka.md
│
├── operations/
│   ├── retention-and-gc.md
│   ├── object-gc-state-machine.md
│   ├── bookkeeper-retention.md
│   ├── failure-and-recovery.md
│   ├── backpressure-and-timeouts.md
│   ├── observability.md
│   └── security-and-multitenancy.md
│
├── examples/
│   ├── pulsar-object-wal-sync.md
│   ├── pulsar-bookkeeper-async-object.md
│   ├── bookkeeper-sync-object-timeout.md
│   └── kafka-transaction-and-delete-records.md
│
├── development/
│   ├── module-boundaries.md
│   ├── source-reading-map.md
│   └── project-status.md
│
└── reference/
    ├── correctness-authorities.md
    ├── glossary.md
    ├── state-machines.md
    ├── end-to-end-cheatsheet.md
    ├── non-goals.md
    └── document-baseline.md
```

## 4.7 Nereus PDF 章节到网站目录的映射

| PDF 内容                               | 网站目标                                                              |
| ------------------------------------ | ----------------------------------------------------------------- |
| 第 1～2 章：阅读说明、为什么需要 Nereus | `docs/nereus/overview/` |
| 第 3 章：stream、record、entry、offset | `docs/nereus/concepts/stream-record-entry-offset.md` 等 |
| 第 4 章：WAL、head、commit、CAS、ReadTarget | `docs/nereus/concepts/primary-wal.md` 到 `read-target-and-offset-index.md` |
| 第 5～6 章：append、结果不确定和恢复 | `docs/nereus/data-paths/append-*` |
| 第 7～8 章：Profile、Object WAL、BK WAL | `docs/nereus/storage/storage-profiles.md`、`object-wal.md`、`bookkeeper-wal.md` |
| 第 9 章：Generation | 拆成 overview、lifecycle、selection/fallback 三页 |
| 第 10 章：通用读路径 | `docs/nereus/data-paths/read-*` |
| 第 11 章：Pulsar 接入 | `docs/nereus/integrations/pulsar*` |
| 第 12 章：Native Kafka 接入 | `docs/nereus/integrations/native-kafka*` |
| 第 13 章：后台物化 | `docs/nereus/storage/materialization.md` |
| 第 14～17 章：GC、恢复、背压、安全 | `docs/nereus/operations/` |
| 第 18 章：正确性所有者 | `docs/nereus/reference/correctness-authorities.md` |
| 第 19 章：代码模块 | `docs/nereus/development/module-boundaries.md` |
| 第 20 章：四个端到端示例 | `docs/nereus/examples/` |
| 第 21 章：Non-goals | `docs/nereus/reference/non-goals.md` |
| 附录 A～E | `docs/nereus/reference/` 和 `docs/nereus/development/source-reading-map.md` |

PDF 本身已经覆盖了写、读、物化和 GC 的一页式速查流程，这一部分非常适合直接转换成 Reference 页面。

---

# 五、内容映射、源码锁与重定向清单

多产品站点需要同时回答三个问题：某个页面来自哪里、基于哪个源码版本、旧链接迁移到哪里。建议把当前迁移文件扩展为：

```text
migration/
├── product-source-locks.yml
├── legacy-redirects.ts
├── nereus/
│   └── pdf-content-map.yml
└── nereus-delay/
    └── content-map.yml
```

当前 `migration/pdf-content-map.yml` 在文档目录迁移时使用 `git mv` 移到 `migration/nereus/pdf-content-map.yml`，不能重新生成后丢失既有覆盖记录。

## 5.1 产品源码锁

`product-source-locks.yml` 是网站展示状态和文档核验的统一索引：

```yaml
products:
  nereus:
    repository: nereusstream/nereus
    branch: main
    commit: c820391dc1de4229362ddf833487066c32609cba
    verified: 2026-08-07
    status: testing
    content_map: migration/nereus/pdf-content-map.yml

  nereus-delay:
    repository: nereusstream/nereus-delay
    branch: main
    commit: 9281890f42772cc01b6b2b607fd93e31de64879b
    verified: 2026-08-07
    spec_revision: V1-FROZEN-2026-08-01
    status: in-development
    content_map: migration/nereus-delay/content-map.yml
```

实施 Nereus Delay 页面前必须再次读取当前 checkout；如果 HEAD 已经前进，则重新记录完整 commit，并把产品页文字与该 commit 的 README、设计、Registry、ADR、Implementation Status、Audit 和 Context 交叉核验。

source lock 不是自动宣称“永远等于最新 main”。它表示网站已经核验到的最新明确基线。推进 commit 时必须同时更新核验日期、状态摘要和受影响页面；只改 SHA 不算完成同步。

## 5.2 Nereus PDF 覆盖表

Nereus PDF 内容很长，覆盖表继续要求每个实质性小节、图和表都有目标页面。目录迁移后的示例：

```yaml
source:
  title: Nereus 当前架构与端到端流程
  repository: nereusstream/nereus
  commit: 894fc4e4d9afcfd5ec14c2bde336106b85c2a151
  verified: 2026-08-04

sections:
  - source: "2.1-2.3"
    target:
      - "docs/nereus/overview/why-nereus.md"
      - "docs/nereus/overview/architecture.md"
    status: migrated

  - source: "3.1-3.7"
    target:
      - "docs/nereus/concepts/stream-record-entry-offset.md"
      - "docs/nereus/concepts/protocol-logical-physical-coordinates.md"
    status: migrated

  - source: "9.1-9.20"
    target:
      - "docs/nereus/storage/generation-overview.md"
      - "docs/nereus/storage/generation-lifecycle.md"
      - "docs/nereus/storage/generation-selection-and-fallback.md"
    status: migrated
```

最终仍必须满足：

```yaml
unmapped_sections: []
unmapped_figures: []
unmapped_tables: []
```

目录移动后只允许 target path 改变，不允许把已经完成的覆盖项重置成未知或静默删除。

## 5.3 Nereus Delay 内容映射

`migration/nereus-delay/content-map.yml` 按权威材料而不是 PDF 页码建立映射：

```yaml
source:
  repository: nereusstream/nereus-delay
  commit: 9281890f42772cc01b6b2b607fd93e31de64879b
  spec_revision: V1-FROZEN-2026-08-01
  verified: 2026-08-07

documents:
  - source: README.md
    targets:
      - docs/nereus-delay/index.mdx
      - docs/nereus-delay/overview/why-nereus-delay.md
    status: migrated

  - source: docs/Nereus Delay V1 设计.md
    sections:
      - source: "1. 摘要"
        targets:
          - docs/nereus-delay/overview/architecture.md
      - source: "3.1 时间"
        targets:
          - docs/nereus-delay/concepts/delivery-time-and-action-time.md

  - source: docs/IMPLEMENTATION-STATUS.md
    targets:
      - docs/nereus-delay/development/project-status.md
    status: summarized-with-source-link
```

每一项状态使用闭合集合：`planned | migrated | summarized-with-source-link | linked-normative-source | not-public`。`not-public` 必须写明理由；不能用它隐藏 non-goal、release blocker 或安全/正确性边界。

## 5.4 旧 URL 重定向

`legacy-redirects.ts` 登记每个已发布 Nereus URL，并可被 Docusaurus 配置和验证脚本直接导入：

```ts
export const legacyDocRedirects = [
  {
    from: '/docs/overview/architecture/',
    to: '/docs/nereus/overview/architecture/',
  },
  {
    from: '/docs/storage/storage-profiles/',
    to: '/docs/nereus/storage/storage-profiles/',
  },
];
```

实现脚本从该文件生成 `@docusaurus/plugin-client-redirects` 配置或等价静态重定向。要求：

* `from` 唯一，`to` 必须是现存 canonical route。
* 不允许重定向链；旧地址一次到达最终地址。
* 不允许把所有旧页面都粗暴重定向到 `/docs/nereus/`。
* 构建后逐项验证 redirect HTML 已生成。
* Sitemap、canonical、首页和正文只暴露新 URL。

## 5.5 通用迁移规则

* 每一个来源标题都必须映射到一个网站标题、明确交叉引用或带理由的非公开项。
* 每一个实质性结论都必须保留。
* 重复背景可以移动到统一页面，但不能静默删除。
* 所有图和表单独登记。
* “代码当前边界”、release blocker、uncertain outcome 和 non-goal 不得为了营销删掉。
* 网站页面必须显示产品、source commit 和 verified date，不能让读者误以为摘要自动对应未来 main。

---

# 六、每个文档页面的统一格式

两个产品的页面采用同一元数据契约，但来源权威可以不同。

## 6.1 Front matter

```md
---
title: Generation
description: How Nereus represents the same logical offset range using multiple physical layouts.
sidebar_position: 4
product: nereus
source_repository: nereusstream/nereus
source_commit: c820391dc1de4229362ddf833487066c32609cba
source_paths:
  - docs/phase-4-compaction-generation/README.md
last_verified: 2026-08-07
status: current-main
authority: reader-facing-summary
---
```

Nereus Delay 示例：

```md
---
title: Delivery time and action time
description: The not-before visibility boundary and the earliest allowed destination action.
sidebar_position: 1
product: nereus-delay
source_repository: nereusstream/nereus-delay
source_commit: 9281890f42772cc01b6b2b607fd93e31de64879b
source_paths:
  - CONTEXT.md
  - docs/Nereus Delay V1 设计.md
last_verified: 2026-08-07
status: current-main
authority: reader-facing-summary
spec_revision: V1-FROZEN-2026-08-01
---
```

实际页面不能保留占位符。URL 由 docs instance 的 `routeBasePath` 和文件 ID 生成，front matter 不重复写 `/docs/nereus` 或 `/docs/nereus-delay` 前缀，避免迁移时出现双重路径。文档 ID、文件名和 heading ID 使用英文；页面标题和正文可以本地化。

字段约束：

| 字段 | 规则 |
| --- | --- |
| `product` | 只能是已登记的产品 ID |
| `source_repository` | 必须与 `product-source-locks.yml` 一致 |
| `source_commit` | 完整 40 位 SHA，不能只写 branch |
| `source_paths` | 至少一个真实存在的来源路径 |
| `last_verified` | ISO `YYYY-MM-DD`，表示内容核验日期，不是文件修改日期 |
| `status` | `current-main`、`released`、`historical` 等闭集值 |
| `authority` | 区分 reader-facing summary、normative mirror、implementation status 等 |
| `spec_revision` | 仅在产品存在独立规范 revision 时使用 |

## 6.2 页面正文模板

```mdx
import DocBaseline from '@site/src/components/DocBaseline';

<DocBaseline
  product="nereus"
  repository="nereusstream/nereus"
  commit="c820391dc1de4229362ddf833487066c32609cba"
  verified="2026-08-07"
  authority="reader-facing-summary"
/>

# Generation

## What problem does it solve? {#problem}

先从为什么需要这个概念讲起。

## Definition {#definition}

给出正式定义。

## What it is not {#not-this}

解释它不是 offset、commitVersion、Leader epoch 等。

## Normal flow {#normal-flow}

正常执行流程。

## Failure and recovery {#failure-recovery}

发生响应丢失、对象损坏或 Broker 切换时如何恢复。

## Source anchors {#source-anchors}

- `GenerationReadResolver.java`
- `GenerationLifecycle.java`
- `docs/phase-4-compaction-generation/README.md`

## Related topics {#related-topics}

- [Read path](../data-paths/read-flow.md)
- [Materialization](./materialization.md)
- [Retention and GC](../operations/retention-and-gc.md)
```

## 6.3 建议新增的 MDX 组件

不需要大量复杂组件。多产品首期组件分成全局组件和产品文档组件：

```text
DocBaseline
ProductBadge
ProductStatus
ArchitectureDiagram
FlowSteps
ProfileMatrix
AuthorityTable
StateMachine
SourceLinks
CompatibilityBoundary
```

`DocBaseline` 必须泛化为产品无关组件：

* 显示 product、repository、commit、verified date 和 authority。
* repository + commit 链接到对应 GitHub tree。
* 当 front matter 与 `product-source-locks.yml` 不一致时由 CI 报错。
* 不把 `last update time` 当成 `last verified`。

`ProductStatus` 从 `src/data/products.ts` 读取全局摘要；产品文档中的详细状态页面可以提供更细的证据，但不能返回更乐观的全局状态。

其中 `CompatibilityBoundary` 用来突出：

```text
Supported
Experimental
Current limitation
Non-goal
```

这样 Native Kafka 的 RF=1 初始边界、在线 Profile 迁移未支持等内容不会埋在正文深处。

---

# 七、产品图表与品牌视觉资产

不要直接截取 PDF 页面作为图片。PDF 截图会有几个问题：

* 小屏幕无法阅读。
* 文本不可搜索。
* 无法适配暗色模式。
* 无法单独链接某个节点。
* 中文和英文版本无法分别翻译。

建议至少重画七张核心图：

| PDF 图                         | 网站形式                      |
| ----------------------------- | ------------------------- |
| Nereus 在 Pulsar 和 Kafka 下的位置  | Nereus 产品页 + Docs Mermaid |
| 通用 Append 流程                  | Mermaid flowchart         |
| Generation 演进                 | Mermaid flowchart         |
| 通用 Read 流程                    | Mermaid flowchart         |
| Pulsar projection 与 MessageId | Mermaid                   |
| Kafka binding 与 ranged entry  | Mermaid                   |
| Object physical root 删除状态机    | Mermaid `stateDiagram-v2` |

PDF 中的通用 Append 流程已经明确从协议 payload、profile 检查、stream lane、WAL、protection、commit intent、head CAS 到完成边界。

Read 流程也已经明确划分了坐标转换、snapshot、generation 解析、reader pin、物理读取和协议结果组装。

Generation 图需要保留“同一个逻辑区间、多个物理表示、旧 generation 不立即删除”的关系。

组织首页不复用这些 Nereus 架构图。组织首页只展示产品关系和真实品牌 mark，避免让访问者误以为 NereusStream 等于 storage core。

Nereus Delay 首期至少需要四张 Mermaid 图：

| 图 | 目的 |
| --- | --- |
| Overall architecture | Command Topic/Shard Log、Shard Runtime、RocksDB、Scheduler、destination adapter、Oxia、Object Store 的边界 |
| Schedule lifecycle | Prepared → queued → applied/rejected → due → publishing/uncertain → terminal |
| Timing boundary | `deliverAt`、`actionAt`、trusted UTC interval、expiration 的关系 |
| Recovery boundary | Recovery Set、Recovery Floor、checkpoint、Shard Log replay 与 GC protection |

Delay 图必须以固定 source commit 的设计和 Registry 为依据，不能只根据 README 画出缺少 uncertainty、owner fencing 或 recovery floor 的简化成功路径。

Docusaurus 官方支持通过 `@docusaurus/theme-mermaid` 直接渲染 Mermaid，Pulsar 当前配置也已经启用了 Mermaid。([Docusaurus][1])

复杂架构图可以使用 SVG，但应保留 `.mmd`、`.drawio` 或其他可编辑源文件，例如：

```text
static/diagrams/
├── nereus/
│   ├── architecture-overview.svg
│   ├── architecture-overview.drawio
│   ├── generation-evolution.svg
│   └── generation-evolution.mmd
└── nereus-delay/
    ├── architecture.mmd
    ├── schedule-lifecycle.mmd
    ├── timing-boundary.mmd
    └── recovery-boundary.mmd
```

---

# 八、工程目录建议

目标工程目录：

```text
nereusstream.github.io/
├── docs/
│   ├── nereus/
│   └── nereus-delay/
├── i18n/
│   └── zh-Hans/
│       ├── docusaurus-plugin-content-docs/
│       ├── docusaurus-plugin-content-docs-nereusDelay/
│       ├── docusaurus-theme-classic/
│       └── code.json
│
├── src/
│   ├── components/
│   │   ├── home/
│   │   │   ├── OrganizationHero/
│   │   │   ├── ProductShowcase/
│   │   │   ├── OrganizationPrinciples/
│   │   │   └── DocumentationGateway/
│   │   ├── products/
│   │   │   ├── nereus/
│   │   │   │   ├── ArchitectureOverview/
│   │   │   │   ├── CoreInvariants/
│   │   │   │   ├── StorageProfiles/
│   │   │   │   └── ProjectStatus/
│   │   │   └── nereus-delay/
│   │   │       ├── DelayArchitecture/
│   │   │       ├── TimingSemantics/
│   │   │       └── DeliveryBoundaries/
│   │   ├── DocBaseline/
│   │   ├── ProductBadge/
│   │   ├── ProductStatus/
│   │   ├── ProfileMatrix/
│   │   ├── AuthorityTable/
│   │   └── SourceLinks/
│   │
│   ├── data/
│   │   ├── products.ts
│   │   ├── storageProfiles.ts
│   │   ├── nereusStatus.ts
│   │   └── nereusDelayStatus.ts
│   │
│   ├── pages/
│   │   ├── index.tsx
│   │   ├── index.module.css
│   │   ├── docs/
│   │   │   ├── index.tsx
│   │   │   └── index.module.css
│   │   └── products/
│   │       ├── nereus/
│   │       │   ├── index.tsx
│   │       │   └── index.module.css
│   │       └── nereus-delay/
│   │           ├── index.tsx
│   │           └── index.module.css
│   │
│   └── css/
│       └── custom.css
│
├── static/
│   ├── img/
│   ├── diagrams/
│   │   ├── nereus/
│   │   └── nereus-delay/
│   └── pdf/
│
├── migration/
│   ├── product-source-locks.yml
│   ├── legacy-redirects.ts
│   ├── nereus/pdf-content-map.yml
│   └── nereus-delay/content-map.yml
│
├── docusaurus.config.ts
├── sidebars.nereus.ts
├── sidebars.nereus-delay.ts
├── package.json
├── tsconfig.json
├── yarn.lock
├── scripts/
│   ├── verify-doc-baselines.mjs
│   ├── verify-content-maps.mjs
│   └── verify-legacy-redirects.mjs
└── .github/
    └── workflows/
        └── pages.yml
```

拆分规则：

* `src/pages/index.tsx` 只编排组织首页 section，不放 Nereus 专属常量。
* `src/data/products.ts` 只存展示和路由元数据，不存大段产品实现说明。
* Nereus 现有 `storageProfiles.ts` 保持产品局部数据，不提升为组织概念。
* 共享组件不能 import 某个产品的 status 文件；由 props 或 `ProductDefinition` 输入。
* 产品页 CSS Module 互相隔离，`custom.css` 只保留 Docusaurus theme token、Navbar、Footer 和文档全局规则。
* 不把新的组织首页再次写成一个数百行组件；每个 section 有清晰组件边界。

---

# 九、Docusaurus 配置建议

多产品首期配置应接近下面的结构；省略 Prism 等与路由无关的现有配置：

```ts
import type {Config} from '@docusaurus/types';
import type {
  Options,
  ThemeConfig,
} from '@docusaurus/preset-classic';
import {legacyDocRedirects} from './migration/legacy-redirects';

const config: Config = {
  title: 'NereusStream',
  tagline: 'Infrastructure for durable streams and scheduled delivery',

  url: 'https://nereusstream.github.io',
  baseUrl: '/',

  organizationName: 'nereusstream',
  projectName: 'nereusstream.github.io',

  trailingSlash: true,
  onBrokenLinks: 'throw',

  markdown: {
    mermaid: true,
    hooks: {
      onBrokenMarkdownLinks: 'throw',
    },
  },

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  themes: ['@docusaurus/theme-mermaid'],

  presets: [
    [
      'classic',
      {
        docs: {
          path: 'docs/nereus',
          routeBasePath: 'docs/nereus',
          sidebarPath: require.resolve('./sidebars.nereus.ts'),
          editUrl:
            'https://github.com/nereusstream/nereusstream.github.io/edit/master/',
          showLastUpdateAuthor: true,
          showLastUpdateTime: true,
          editLocalizedFiles: true,
        },

        blog: false,

        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      } satisfies Options,
    ],
  ],

  plugins: [
    [
      '@docusaurus/plugin-content-docs',
      {
        id: 'nereusDelay',
        path: 'docs/nereus-delay',
        routeBasePath: 'docs/nereus-delay',
        sidebarPath: require.resolve('./sidebars.nereus-delay.ts'),
        editUrl:
          'https://github.com/nereusstream/nereusstream.github.io/edit/master/',
        showLastUpdateAuthor: true,
        showLastUpdateTime: true,
      },
    ],
    [
      '@docusaurus/plugin-client-redirects',
      {
        redirects: legacyDocRedirects,
      },
    ],
  ],

  themeConfig: {
    navbar: {
      title: 'NereusStream',
      logo: {
        alt: 'NereusStream',
        src: 'img/nereus-icon.png',
        width: 42,
        height: 21,
      },
      items: [
        {
          type: 'dropdown',
          label: 'Products',
          position: 'left',
          items: [
            {label: 'Nereus', to: '/products/nereus/'},
            {label: 'Nereus Delay', to: '/products/nereus-delay/'},
          ],
        },
        {
          label: 'Documentation',
          to: '/docs/',
          position: 'left',
        },
        {
          href: 'https://github.com/nereusstream',
          position: 'right',
          className: 'header-github-link',
          'aria-label': 'NereusStream GitHub organization',
        },
      ],
    },

    docs: {
      sidebar: {
        hideable: true,
        autoCollapseCategories: true,
      },
    },

    footer: {
      style: 'dark',
      links: [
        {
          title: 'Products',
          items: [
            {label: 'Nereus', to: '/products/nereus/'},
            {label: 'Nereus Delay', to: '/products/nereus-delay/'},
          ],
        },
        {
          title: 'Documentation',
          items: [
            {label: 'Documentation home', to: '/docs/'},
            {label: 'Nereus docs', to: '/docs/nereus/'},
            {label: 'Nereus Delay docs', to: '/docs/nereus-delay/'},
          ],
        },
        {
          title: 'Organization',
          items: [
            {label: 'GitHub', href: 'https://github.com/nereusstream'},
            {
              label: 'Website source',
              href: 'https://github.com/nereusstream/nereusstream.github.io',
            },
          ],
        },
      ],
      copyright:
        `Copyright © ${new Date().getFullYear()} NereusStream.`,
    },
  } satisfies ThemeConfig,
};

export default config;
```

新增依赖使用与 Docusaurus core 相同的精确版本，避免 plugin/core 漂移：

```bash
yarn add @docusaurus/plugin-content-docs@3.10.2 \
  @docusaurus/plugin-client-redirects@3.10.2
```

如果 `plugin-content-docs` 已作为传递依赖存在，仍应把它写入本项目直接 dependencies，因为配置文件已经直接引用该 plugin。

因为这是组织主页仓库，GitHub Pages 地址位于站点根路径，因此应使用 `baseUrl: '/'`，而不是 `/nereusstream.github.io/`。Docusaurus 的 GitHub Pages 部署说明也专门区分了组织主页仓库和普通项目仓库。([Docusaurus][2])

---

# 十、两个产品侧边栏必须显式定义

Docusaurus 支持自动生成侧边栏，但两个产品都不适合完全按文件名排序。Nereus 的概念存在强依赖：

```text
offset
  -> WAL
  -> commit
  -> generation
  -> read selection
  -> retention / GC
```

如果文件按字母顺序出现，读者可能在理解 offset 和提交点之前就进入 generation 或 GC。

`sidebars.nereus.ts` 保留当前阅读顺序：

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const nereusSidebars: SidebarsConfig = {
  docsSidebar: [
    'index',

    {
      type: 'category',
      label: 'Overview',
      link: {
        type: 'generated-index',
        title: 'Nereus overview',
      },
      items: [
        'overview/why-nereus',
        'overview/architecture',
        'overview/reading-guide',
      ],
    },

    {
      type: 'category',
      label: 'Concepts',
      items: [
        'concepts/stream-record-entry-offset',
        'concepts/protocol-logical-physical-coordinates',
        'concepts/primary-wal',
        'concepts/stream-head-and-commit-chain',
        'concepts/cas-and-linearization-point',
        'concepts/read-target-and-offset-index',
      ],
    },

    {
      type: 'category',
      label: 'Write and Read Paths',
      items: [
        'data-paths/append-flow',
        'data-paths/append-outcomes',
        'data-paths/append-recovery',
        'data-paths/read-flow',
        'data-paths/read-boundaries',
        'data-paths/index-repair-and-fallback',
      ],
    },

    {
      type: 'category',
      label: 'Storage and Physical Evolution',
      items: [
        'storage/storage-profiles',
        'storage/object-wal',
        'storage/bookkeeper-wal',
        'storage/generation-overview',
        'storage/generation-lifecycle',
        'storage/generation-selection-and-fallback',
        'storage/materialization',
      ],
    },

    {
      type: 'category',
      label: 'Protocol Integrations',
      items: [
        'integrations/pulsar',
        'integrations/pulsar-cursor-and-subscription',
        'integrations/native-kafka',
        'integrations/kafka-produce-and-fetch',
        'integrations/kafka-recovery-and-compaction',
        'integrations/kop-vs-native-kafka',
      ],
    },

    {
      type: 'category',
      label: 'Operations and Recovery',
      items: [
        'operations/retention-and-gc',
        'operations/object-gc-state-machine',
        'operations/bookkeeper-retention',
        'operations/failure-and-recovery',
        'operations/backpressure-and-timeouts',
        'operations/observability',
        'operations/security-and-multitenancy',
      ],
    },

    {
      type: 'category',
      label: 'Examples',
      items: [
        'examples/pulsar-object-wal-sync',
        'examples/pulsar-bookkeeper-async-object',
        'examples/bookkeeper-sync-object-timeout',
        'examples/kafka-transaction-and-delete-records',
      ],
    },

    {
      type: 'category',
      label: 'Reference',
      items: [
        'reference/correctness-authorities',
        'reference/glossary',
        'reference/state-machines',
        'reference/end-to-end-cheatsheet',
        'reference/non-goals',
        'reference/document-baseline',
        'development/module-boundaries',
        'development/source-reading-map',
      ],
    },
  ],
};

export default nereusSidebars;
```

`sidebars.nereus-delay.ts` 首期使用独立顺序：

```ts
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const nereusDelaySidebars: SidebarsConfig = {
  docsSidebar: [
    'index',
    {
      type: 'category',
      label: 'Overview',
      items: [
        'overview/why-nereus-delay',
        'overview/architecture',
        'overview/reading-guide',
      ],
    },
    {
      type: 'category',
      label: 'Concepts',
      items: [
        'concepts/delivery-time-and-action-time',
        'concepts/commands-messages-and-receipts',
        'concepts/shard-log-and-source-position',
        'concepts/managed-and-auto-fast',
        'concepts/delivery-guarantees-and-uncertainty',
      ],
    },
    {
      type: 'category',
      label: 'Data paths',
      items: [
        'data-paths/schedule-flow',
        'data-paths/cancel-and-reschedule',
        'data-paths/due-claim-and-publish',
        'data-paths/query-and-read-barrier',
      ],
    },
    {
      type: 'category',
      label: 'Operations',
      items: [
        'operations/destination-lane-isolation',
        'operations/ownership-and-activation',
        'operations/checkpoints-and-recovery',
        'operations/payload-lifecycle-and-gc',
        'operations/observability-and-release-gates',
      ],
    },
    {
      type: 'category',
      label: 'Reference',
      items: [
        'reference/glossary',
        'reference/authority-order',
        'reference/protocol-registry-guide',
        'reference/non-goals',
        'development/project-status',
      ],
    },
  ],
};

export default nereusDelaySidebars;
```

实施某一批文档时，sidebar 只能登记这一批已经存在的页面；不能先提交完整 sidebar 再留下 broken link。Pulsar 本身也使用显式结构化 sidebar，而不是完全依赖目录扫描。Docusaurus 同时支持显式侧边栏、生成式分类索引、可隐藏侧边栏和自动折叠。([Docusaurus][3])

---

# 十一、中英文处理

旧静态首页曾使用 `data-i18n` 属性和自定义切换逻辑；活动 Docusaurus 页面不得恢复这种页面内翻译字典。多产品翻译统一使用 Docusaurus i18n。

## 11.1 推荐语言策略

由于 NereusStream 面向公开开源社区，最终建议：

```ts
defaultLocale: 'en',
locales: ['en', 'zh-Hans'],
```

路径表现为：

```text
English Nereus:       /docs/nereus/...
中文 Nereus:          /zh-Hans/docs/nereus/...
English Nereus Delay: /docs/nereus-delay/...
中文 Nereus Delay:    /zh-Hans/docs/nereus-delay/...
```

但不要在英文文档还大量缺失时提前展示语言切换，否则会出现英文和中文混杂。

更实际的迁移顺序是：

1. 先完成英文组织首页、Docs Hub、两个产品页和两个产品 Overview。
2. 迁移 Nereus 已有中文 PDF 内容到 Nereus 中文实例。
3. 从 Nereus Delay 中文设计中整理读者层中文页面，同时保持 Registry 精确术语。
4. 页面 ID、文件路径、URL slug 在两种语言中保持一致。
5. 两个产品的入口级页面都可用后再显示 locale dropdown。

Docusaurus 的正文翻译按完整 Markdown/MDX 文件维护，主题、导航和 Footer 则使用 JSON 翻译文件。([Docusaurus][4])

中文文档目录类似：

```text
i18n/
└── zh-Hans/
    ├── docusaurus-plugin-content-docs/
    │   └── current/
    │       ├── index.mdx
    │       ├── overview/
    │       └── ...
    └── docusaurus-plugin-content-docs-nereusDelay/
        └── current/
            ├── index.mdx
            ├── overview/
            └── ...
```

## 11.2 所有标题增加显式 ID

不要依赖中文标题自动产生 anchor：

```md
## 什么是 Generation {#generation-definition}
```

而不是：

```md
## 什么是 Generation
```

否则英文和中文标题产生的 anchor 不同，跨语言链接容易失效。Docusaurus 的本地化教程也建议为翻译文档使用显式 heading ID。([Docusaurus][5])

---

# 十二、文档版本策略

现在不要立刻复制 Pulsar 的多版本文档体系。两个产品的版本生命周期独立，不能用一个全局版本号覆盖。

当前最简单可靠的做法是：

```text
/docs/nereus/       = 已核验的 Nereus main 文档
/docs/nereus-delay/ = 已核验的 Nereus Delay main 文档

每页显示 product + repository + full commit + verified date + authority
```

Nereus `v0.1.0` 正式发布且文档与 next 分支出现实质差异后，默认实例可以执行：

```bash
yarn docusaurus docs:version 0.1
```

Nereus Delay 独立发布并确实需要冻结旧文档时，针对其 plugin ID 执行：

```bash
yarn docusaurus docs:version:nereusDelay 1.0
```

版本化后的目标仍位于各自产品命名空间，不创建含义不清的全局 `/docs/next`：

```text
/docs/nereus/...
/docs/nereus-delay/...
```

版本选择器必须绑定对应 `docsPluginId`，只在产品文档上下文显示。Docs Hub 和组织首页显示产品状态，不显示跨产品共享版本号。

Docusaurus 官方也明确提醒，过早版本化会增加重复文件、构建时间和维护成本；只有不同版本之间确实存在显著文档差异时才值得启用。([Docusaurus][6])

---

# 十三、搜索

Nereus 文档已经超过首期小规模目录；Nereus Delay 加入后，应把搜索作为多产品阶段的后续必做项，但不阻塞品牌和路由迁移。

推荐顺序：

1. 网站先上线。
2. 申请 Algolia DocSearch。
3. 配置 `themeConfig.algolia`。
4. 打开 `contextualSearch`。
5. 为 Nereus 与 Nereus Delay 设置可区分的 product facet 或层级路径。
6. 让搜索按照当前产品、语言和版本优先过滤，同时允许用户切换到全站结果。

Docusaurus 对 Algolia DocSearch 提供一级支持，classic preset 不需要额外安装搜索主题；其 contextual search 还能区分当前语言和文档版本。([Docusaurus][7])

在 Algolia 尚未批准前，不建议为了临时搜索引入多个社区插件。Docs Hub、产品 sidebar 和浏览器原生站内导航足够支撑本轮结构迁移。

---

# 十四、网站、产品仓库与 PDF 的权威关系

多产品以后不能用一句“Markdown 是唯一源”覆盖所有内容。应形成分层关系：

```text
Product repositories
├── implementation
├── normative design / protocol / ADR
└── implementation status and evidence
             │ pinned commit + content map
             ▼
nereusstream.github.io
├── organization and product landing pages
├── reader-facing architecture/concept/operation docs
├── source baseline and authority labels
├── search/i18n pages
└── per-product release PDF snapshots
```

禁止回到：

```text
one PDF or one copied Markdown tree
    -> silently treated as every product's current truth
```

文档所有权：

| 内容 | 权威维护位置 | 网站处理方式 |
| --- | --- | --- |
| 组织定位、产品入口、跨产品导航 | `nereusstream.github.io` | 直接维护 |
| 面向读者的架构、概念、流程、运维说明 | `nereusstream.github.io` | 基于 product source lock 核验 |
| 类级设计、实现合同、代码行为 | 对应产品仓库 | 固定 commit 链接和摘要 |
| Nereus Delay V1 精确协议 | Nereus Delay Design + Registry + Accepted ADR | 不通过摘要放宽；完整镜像需自动校验 |
| 当前实现状态和测试证据 | 对应产品仓库的状态文档与代码 | 网站只做带 baseline 的摘要 |
| Content map、redirect、网站 source lock | `nereusstream.github.io/migration` | CI 校验 |
| PDF | 每个产品从已核验网站 Markdown 生成的发布快照 | 版本或 commit 命名 |

PDF 不建议每次正文修改都直接提交进 Git 历史。更好的方式是按产品发布：

* Nereus `v0.1.0` 发布时生成 Nereus PDF。
* Nereus Delay V1 发布时生成独立 Delay PDF。
* 作为 GitHub Release asset 上传。
* 对应产品 Docs 首页提供 “Download PDF snapshot”。
* 文件名包含版本或 commit：

```text
nereus-architecture-v0.1.0.pdf
nereus-architecture-main-c820391d.pdf
nereus-delay-v1-design-<release-commit>.pdf
```

当前 `Nereus-当前架构与端到端流程-完整版.pdf` 继续作为 Nereus 初始迁移证据，不代表 Nereus Delay，也不代表未来产品。以后不要手工维护一份与网站 Markdown 平行演进、无法校验的 PDF 文本。

---

# 十五、GitHub Pages 自动部署

仓库继续使用：

```text
Settings
  -> Pages
  -> Source
  -> GitHub Actions
```

工作流可以采用：

```yaml
name: Build and deploy site

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Verify documentation contracts
        run: yarn docs:check

      - name: Type check
        run: yarn typecheck

      - name: Build
        run: yarn build

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: build

  deploy:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
```

`fetch-depth: 0` 是为了让 Docusaurus 能读取完整 Git 历史并显示真实的最后修改时间。

当前 GitHub 官方 Pages 文档推荐通过 `configure-pages`、`upload-pages-artifact` 和 `deploy-pages` 构建自定义 Pages 工作流，并要求部署任务具备 `pages: write` 与 `id-token: write` 权限。([GitHub Docs][8]) 当前官方 Checkout 和 setup-node 主版本也已经分别进入 v6。([GitHub][9])

PR 阶段只构建，不部署；合并到 `master` 后才部署。

`package.json` 增加：

```json
{
  "scripts": {
    "docs:check": "node scripts/verify-doc-baselines.mjs && node scripts/verify-content-maps.mjs && node scripts/verify-legacy-redirects.mjs"
  }
}
```

三个验证脚本至少检查：

* 每个产品和文档页面的 source commit 是完整 SHA，verified date 格式正确。
* front matter 的产品、仓库和 commit 与 `product-source-locks.yml` 一致。
* content map 的 target 文件全部存在，Nereus 的 unmapped 集合为空。
* Nereus Delay 映射状态属于闭合集合，所有公开页面至少有一个来源。
* redirect `from` 唯一、`to` 路由存在、没有链式跳转。
* 正文、Navbar、Footer 和产品数据中没有遗留 canonical `/docs/overview/...` 等旧链接。

常规 Pages 构建只校验已提交的 source lock，不在每次构建时自动追随产品 main。需要确认产品 main 是否前进时，使用单独的只读检查或定期工作流报告 drift；不能自动推进 SHA 并在未核验内容的情况下直接部署。

---

# 十六、多产品改造的实施批次与提交策略

原 Docusaurus 框架迁移、Nereus 首页和 PDF 文档迁移已经完成，不再重复执行。新的实施从下面批次开始。每个批次都必须保持可构建、可回退，并在验证通过后单独 commit、push；不允许把所有改动积压成最终一次提交。

## 批次 0：冻结多产品方案

建议 commit：

```text
docs: define NereusStream multi-product site plan
```

范围：

* 更新本文件的品牌层级、URL、页面结构和文档架构。
* 明确 Nereus Delay 权威材料和状态边界。
* 定义后续批次、验收门和提交策略。
* 不修改运行时代码和当前路由。

验证：`git diff --check`、标题结构检查、旧单产品结论冲突扫描。完成后立即 push。

## 批次 1：品牌与产品数据基础（已完成）

建议 commit：

```text
site: establish NereusStream product navigation
```

范围：

* `docusaurus.config.ts` 的全站 title/tagline 改为 NereusStream。
* Navbar 增加 Products dropdown，GitHub 改为 organization。
* Footer 改为 Products、Documentation、Organization 三组。
* 新增 `src/data/products.ts`，录入 Nereus 与 Nereus Delay 的展示元数据。
* 保持当前 Nereus Docs 入口可访问；Docs Hub 尚未建立时不能提前制造冲突路由。
* 更新本文件中的实际实施状态。

实施结果：`src/data/products.ts` 已登记 Nereus 与 Nereus Delay 的产品路径、文档路径、仓库、完整 source commit、状态和核验日期；配置中的 Products dropdown、产品 Footer、站点 title/tagline 和组织级 GitHub 入口已经接入该数据模型。由于 Docs Hub 尚未建立，Documentation 和 Nereus docs Footer 暂时仍指向现有 `/docs`，没有提前占用 `/docs/` 路由。

验证：

```bash
yarn typecheck
yarn build
```

手工检查 320、375、768、1440px Navbar；品牌不裁切、Logo 不变形、Products 菜单可键盘操作。完成后 commit、push。

## 批次 2：组织首页与 Nereus 产品页（已完成）

建议 commit：

```text
site: split organization home from Nereus product page
```

范围：

* 将当前 Nereus 首页移动到 `/products/nereus/`。
* 修正 Nereus 页面 Docs/GitHub/metadata。
* 新建组织首页：Organization Hero、Products、Organization principles、Documentation gateway。
* 首页所有产品卡片从 `products.ts` 读取。
* 保留 Nereus 现有架构、profile、status 内容，不在迁移中改写其语义。
* 更新本文件实施状态。

实施结果：当前 Nereus 首页已迁移到 `src/pages/products/nereus/` 并保留原有架构、不变量、五种 Storage Profile、文档阅读顺序和 source/PDF baseline；`src/pages/index.tsx` 现在只负责 NereusStream 组织 Hero、产品发现、组织原则、文档入口和组织 CTA。`/products/nereus-delay/` 已先提供由 `products.ts` 驱动的准确定位页，作为批次 4 完整内容接入前的可构建入口。

验证：

* `/` 与 `/products/nereus/` 各自只有一个 H1。
* 首页不再出现 Nereus-only 的五 profile、source SHA 或 PDF SHA。
* Nereus 产品页原有核心内容无遗漏。
* 明暗主题和移动端 section 顺序正确。
* `yarn typecheck && yarn build` 通过。

完成后 commit、push。

## 批次 3：文档命名空间、Docs Hub 与重定向（已完成）

建议 commit：

```text
docs: namespace product documentation and preserve legacy URLs
```

范围：

* 增加 `@docusaurus/plugin-content-docs` 和 `@docusaurus/plugin-client-redirects` 的显式依赖。
* `git mv` 当前 `docs/*` 到 `docs/nereus/*`。
* `sidebars.ts` 移为 `sidebars.nereus.ts`。
* 默认 docs instance 改为 `/docs/nereus/`。
* 新建 `/docs/` Docs Hub，首批展示 Nereus 和即将接入的 Nereus Delay 状态。
* 创建完整 `migration/legacy-redirects.ts`。
* 移动并更新 `migration/nereus/pdf-content-map.yml` target path。
* 更新 Navbar、Footer、Nereus 产品页和全部内部链接。
* 不在这一批同时大量改写正文，降低移动与内容修改混杂的风险。

实施结果：Nereus 文档文件和 sidebar 已移动到 `docs/nereus/` 与 `sidebars.nereus.ts`；默认 docs instance 使用 `routeBasePath: 'docs/nereus'`，`src/pages/docs/index.tsx` 提供独立 Docs Hub；`@docusaurus/plugin-client-redirects` 使用 `migration/legacy-redirects.ts` 为现有正文页和已生成的 Overview 分类页提供重定向。`migration/nereus/pdf-content-map.yml` 的所有 target 已随目录移动，旧 canonical site links 已改为 `/docs/nereus/...`。批次 3 暂不创建 Delay docs plugin，避免在 Delay 来源核验前产生空 sidebar。

验证：

* `yarn typecheck && yarn build` 通过。
* 旧 `/docs/...` 每个页面都有一跳重定向。
* 新 `/docs/nereus/...` 页面、sidebar、上一页/下一页和 Edit 链接正常。
* PDF content map 仍为零 unmapped。
* `rg` 检查没有把旧 route 继续当作 canonical link。

完成后 commit、push。

## 批次 4：Nereus Delay 来源核验、产品页和最小文档实例（已完成）

建议 commit：

```text
site: add Nereus Delay product entry and documentation
```

范围：

* 读取 Nereus Delay 当前 main commit，并核验 README、设计、Registry、ADR、Implementation Status、Audit、Context。
* 新增 `product-source-locks.yml` 中的完整 Delay baseline。
* 新增 Nereus Delay 产品页。
* 配置 `nereusDelay` docs plugin instance 和 `sidebars.nereus-delay.ts`。
* 首批提交 Delay `index.mdx`、Why、Architecture、Authority order、Glossary、Project status。
* 新增 `migration/nereus-delay/content-map.yml`。
* 产品页与首页状态使用同一数据，不声称 release blocker 已关闭。
* 同步更新本文件实施状态。

实施结果：已复核 Nereus Delay `main@9281890f42772cc01b6b2b607fd93e31de64879b`、`V1-FROZEN-2026-08-01` 以及 README、V1 Design、Protocol Registry、ADR index、Implementation Status、V1 Design Audit、CONTEXT。`migration/product-source-locks.yml` 同时登记 Nereus 和 Nereus Delay，`migration/nereus-delay/content-map.yml` 记录权威材料到公开页面的映射。产品页已覆盖 Schedule、Cancel/Reschedule、Payload Commit、Query、端到端 flow、`deliverAt`/`actionAt`、四类结果边界、Lane isolation、Recovery Floor 和当前 blocker；Delay docs instance 已提交最小阅读集合及 Registry/authority/status 入口。

验证：

* 所有 Delay 公开结论都有 source path 和 commit。
* `deliverAt`、queued/applied、uncertain、at-least-once、Lane isolation、Recovery Floor 的边界没有被营销文案弱化。
* `/products/nereus-delay/` 与 `/docs/nereus-delay/` 可访问。
* `yarn typecheck && yarn build` 通过。

完成后 commit、push。

## 批次 5：Nereus Delay 核心阅读路径

建议按内容依赖拆成至少两个 commit，而不是一次塞入全部页面：

```text
docs: add Nereus Delay timing and command model
docs: add Nereus Delay delivery recovery and operations guides
```

第一提交：

* Delivery time / Action time / Trusted UTC interval。
* Commands、messages、receipts、Source Position。
* Managed 与 AUTO_FAST。
* Schedule、Cancel、Reschedule 和 Query 路径。

第二提交：

* Due/Claim/Publish 与 uncertainty。
* Destination Lane isolation。
* Ownership/activation、checkpoint/recovery、payload lifecycle/GC。
* Observability 和 release gates。

每个提交都更新 content map、sidebar 和 project status，并分别 typecheck、build、commit、push。

## 批次 6：文档契约、可访问性和发布加固

建议 commit：

```text
site: enforce multi-product documentation contracts
```

范围：

* 泛化 `DocBaseline`。
* 实现 `docs:check` 三个验证脚本。
* 把检查加入 GitHub Pages build job。
* 补齐 metadata、canonical、sitemap 和社交分享摘要。
* 检查键盘导航、焦点、对比度、状态双重编码、图表文字说明。
* 验证 320、375、768、996、1440px 关键断点。
* 更新本文件和两个产品 project status。

门禁：

```bash
yarn docs:check
yarn typecheck
yarn build
git diff --check
```

完成后 commit、push，并核对本地 HEAD 与远端 `master` SHA 一致。

## 后续独立批次

国际化、搜索和版本化不阻塞上述多产品首版，分别实施：

```text
site: add multi-product Chinese localization
site: add product-aware documentation search
site: cut Nereus v0.1 documentation
site: cut Nereus Delay v1 documentation
```

每个后续批次继续遵循产品独立版本、source baseline 和单批次 commit/push 规则。

---

# 十七、验收标准

多产品首版完成时至少满足以下门禁。

### 品牌和全局导航

* 浏览器 title、Navbar、Footer 和版权主体均为 `NereusStream`。
* 左上角 mark 比例正确、`NereusStream` 文字完整、320px 下不裁切。
* Products 下拉包含 Nereus 与 Nereus Delay，链接到独立产品页。
* Documentation 指向 `/docs/`，GitHub 指向 organization。
* 全局导航不把 Nereus 仓库或 Nereus 状态冒充组织级入口。

### 组织首页

* Hero 标题是 NereusStream，且组织定位同时覆盖 storage 与 delayed scheduling。
* Nereus 是 featured 产品，Nereus Delay 是同级产品；DOM 和 URL 中不存在把 Delay 嵌套进 Nereus 的结构。
* 首页不展示 Nereus-only 的五 profile、generation、source SHA 或 PDF SHA。
* 产品卡片状态、路径和仓库由 `products.ts` 驱动。
* 不存在空的 Coming soon、虚构客户、未验证性能数字或不存在的社区入口。

### 产品页

* `/products/nereus/` 完整保留原 Nereus 首页的架构、不变量、profile、文档和 status 内容。
* `/products/nereus-delay/` 的定位、流程、时间语义、uncertainty、隔离、恢复和状态均有固定源码依据。
* 每个产品页有自己的 title、description、Docs CTA 和 repository CTA。
* Nereus Delay 在 release blocker 未关闭时不显示 stable/ready/complete。

### 文档、路由与阅读顺序

* `/docs/` 是 Docs Hub，不显示某个产品的 sidebar。
* `/docs/nereus/` 与 `/docs/nereus-delay/` 使用独立 plugin instance 和 sidebar。
* Nereus 的所有旧 `/docs/...` URL 一跳到对应新页面，不产生 404 或重定向链。
* `migration/nereus/pdf-content-map.yml` 中 sections、figures、tables 均无未映射项。
* Nereus Delay 首批文档满足第 4.3 节最小集合，每页至少有一个真实 source path。
* 所有状态机和图表有文字说明，不能只依赖图。
* Non-goal、当前能力边界、uncertain outcome 和 release blocker 完整保留。

Nereus 文档继续使用 PDF 给出的三项 review 判断：状态是在描述逻辑数据还是物理表示；它是权威事实还是可修复派生状态；响应丢失后凭什么识别同一次操作。Nereus Delay 还需额外判断：结果是 queued、applied、published 还是 uncertain；时间是 `deliverAt`、`actionAt` 还是观测时间；状态能否从允许的 checkpoint 与 Shard Log 确定性恢复。

### 来源、状态与同步

* `product-source-locks.yml` 包含两个产品的完整 SHA 和核验日期。
* 页面 front matter、`DocBaseline`、`products.ts` 和 project status 不互相矛盾。
* Nereus Delay 网站摘要不覆盖 Design/Registry/ADR 的权威，也不把 Implementation Status 当作规范放宽依据。
* 每一批代码变更都同步更新本文件及受影响 content map/status，不留“最后统一补文档”的债务。

### 工程和发布

* `yarn docs:check`、`yarn typecheck`、`yarn build` 和 `git diff --check` 通过。
* 内部 Markdown link、缺失 sidebar page、无效 redirect 和 baseline mismatch 直接使 CI 失败。
* PR 只构建；`master` push 自动部署。
* 320、375、768、996、1440px 下无水平滚动、重叠、裁切或错误卡片顺序。
* 键盘可以操作 Products、Docs、Theme 和所有 CTA；焦点清晰。
* 状态不只靠颜色表达，图像有 alt，装饰图片使用空 alt。
* 每个实施批次都有独立 commit 和 push，最终本地/远端 SHA 一致。

---

# 十八、不建议做的事情

1. **不要只在当前 Nereus 首页底部追加一张 Nereus Delay 卡片。**
   这样浏览器 title、Hero、Docs、Footer 和全局 GitHub 仍然是单产品模型，没有解决品牌层级。

2. **不要把 NereusStream 当作 Nereus 的长名称。**
   NereusStream 是组织，Nereus 是产品；metadata、Logo alt、URL 和文案都必须维持这一层级。

3. **不要把每个产品都平铺成顶级导航项。**
   使用 Products dropdown 和数据驱动产品模型，避免第三个产品出现时再次重构 Navbar。

4. **不要在 Navbar 使用纵向完整 Logo 并强制压缩。**
   首期使用自然比例 mark + DOM 文字，保证窄屏可读和可访问。

5. **不要把 Nereus Delay 的完整 Registry、ADR 和状态文档手工复制后独立维护。**
   精确契约应固定 commit 链接或自动同步校验；网站摘要不能成为另一个漂移的规范。

6. **不要用一套全局版本号描述两个产品。**
   Nereus 与 Nereus Delay 独立发布、独立 docs plugin、独立 source baseline。

7. **不要展示空的 Future product 或 Coming soon 卡片。**
   新产品必须有真实定位、页面、文档入口、仓库、状态和 baseline 后才加入产品数据。

8. **不要直接 fork `apache/pulsar-site` 后删除无关内容。**
   它包含大量 Pulsar 专用插件、客户端文档、ASF 页面、重定向和版本构建逻辑，清理成本可能高于重新初始化一个 Docusaurus 项目。

9. **不要把 PDF 每页转换为一个网页。**
   PDF 页码不是稳定的信息架构，页面拆分应依据概念和用户任务。

10. **不要把整个 PDF 转成一个 Markdown。**
   这只会得到一个无法导航、无法维护的超长页面。

11. **不要把所有内部 phase/Future 文档都复制到网站。**
   对外网站放稳定架构说明；代码级实现合同继续放在对应产品仓库。

12. **不要制造未经真实部署验证的 Quickstart。**
   安装、配置、启动和故障排查必须从当前代码和实际部署流程验证，不能从架构文档推测。

13. **不要过早维护多个文档版本。**
   先稳定两个产品的 `current`，分别正式发布后再冻结对应版本。

14. **不要同时手工维护网站摘要和 PDF 两套读者层内容。**
   PDF 从对应产品已核验的 Markdown/MDX 生成；精确规范继续由产品仓库维护。

---

# 最终推荐形态

```text
NereusStream Homepage (/)
├── Organization Hero
├── Products
│   ├── Nereus (featured)
│   └── Nereus Delay
├── Organization principles
├── Documentation gateway
└── Footer

Navbar
├── NereusStream mark + title
├── Products dropdown
├── Documentation
├── Search
├── Language
└── GitHub organization

Product pages
├── /products/nereus/
│   ├── Nereus architecture
│   ├── Core invariants
│   ├── Storage profiles
│   ├── Documentation entry cards
│   └── Current status
└── /products/nereus-delay/
    ├── Product positioning
    ├── Schedule/delivery flow
    ├── Timing and outcome boundaries
    ├── Isolation and recovery
    └── Current status

Documentation (/docs/)
├── Nereus docs (/docs/nereus/)
│   ├── Overview
│   ├── Concepts
│   ├── Write and Read Paths
│   ├── Storage and Generation
│   ├── Pulsar and Native Kafka
│   ├── Operations and Recovery
│   ├── Examples
│   └── Reference
└── Nereus Delay docs (/docs/nereus-delay/)
    ├── Overview
    ├── Concepts
    ├── Data paths
    ├── Operations
    ├── Development status
    └── Reference
```

当前最先实施的是 **品牌与产品数据基础**，随后依次完成 **组织首页与 Nereus 产品页拆分**、**文档命名空间和旧 URL 重定向**、**Nereus Delay 产品页与文档实例**。每个阶段按第十六章单独验证、commit、push，并同步更新本方案和内容基线。

[1]: https://docusaurus.io/docs/3.8.1/markdown-features/diagrams "https://docusaurus.io/docs/3.8.1/markdown-features/diagrams"
[2]: https://docusaurus.io/docs/next/deployment/github-pages "https://docusaurus.io/docs/next/deployment/github-pages"
[3]: https://docusaurus.io/docs/sidebar "https://docusaurus.io/docs/sidebar"
[4]: https://docusaurus.io/docs/i18n/introduction "https://docusaurus.io/docs/i18n/introduction"
[5]: https://docusaurus.io/fr/docs/i18n/tutorial "https://docusaurus.io/fr/docs/i18n/tutorial"
[6]: https://docusaurus.io/docs/versioning "https://docusaurus.io/docs/versioning"
[7]: https://docusaurus.io/docs/search "https://docusaurus.io/docs/search"
[8]: https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages "https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages"
[9]: https://github.com/actions/checkout "https://github.com/actions/checkout"
