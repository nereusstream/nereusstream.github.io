# Legacy static homepage

`index.html` is retained here as the pre-Docusaurus homepage snapshot. It is not part of the generated site. The active homepage lives in `src/pages/index.tsx` and is built by Docusaurus.
